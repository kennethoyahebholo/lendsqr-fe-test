import React from 'react'
import './activate.scss'

type Props = {}
// work on the component to make in active, inactive, pending, blacklisted if time permits
const Activate = (props: Props) => {
  return (
    <div className='activate-comp'>
     <h4>Blacklisted</h4>
    </div>
  )
}

export default Activate